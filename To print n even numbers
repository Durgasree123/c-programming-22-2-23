#include <stdio.h>
int main() {
    int n, c=0; i = 0;
    scanf("%d", &n);
    while (c < n) {
        if (i % 2 == 0) {
            printf("%d ", i);
            c++;
        }
        i++;
    }
    getch();
}
